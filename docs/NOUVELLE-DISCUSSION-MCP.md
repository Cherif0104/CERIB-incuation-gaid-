# Lancer une nouvelle discussion pour régler le MCP Supabase

## 1. Ouvrir une nouvelle discussion

- **Ctrl+L** (ou **Cmd+L** sur Mac) pour le chat, puis **New Chat**
- **ou** cliquer sur **+** / **New conversation** dans le panneau de chat.

---

## 2. Message à coller dans cette nouvelle discussion

Copier-coller exactement ceci :

```
On règle le MCP Supabase pour le projet Savana (CERIP Incubation).

1) Vérifie que le serveur MCP "supabase" est disponible dans cette session (liste les serveurs ou appelle un outil Supabase). Note : il peut s'appeler "user-supabase" selon la config Cursor.
2) Si oui : exécute les migrations SQL dans l'ordre décrit dans docs/CONTINUITE-PROJET-SAVANA.md (section 4) : schema.sql, migration-invitations.sql, rls-invitation_codes.sql, migration-coaching-requests.sql, seed.sql. Le projet Supabase est mfxskmfwongxxuqiubcz.
3) Si le MCP supabase n'est pas dispo : dis-moi exactement quoi faire pour le connecter (config Cursor, variables, etc.) et on règle ça.

Contexte complet du projet : lire docs/CONTINUITE-PROJET-SAVANA.md.
```

---

En ouvrant cette nouvelle discussion et en collant ce message, l’agent pourra soit utiliser le MCP Supabase s’il est disponible, soit vous guider pour le régler.
